import React from 'react';
import { Value as SlateValue } from 'slate';
import { BlockContentFeature, BlockContentFeatures, Path, SlateEditor } from '../typeDefs';
declare type AnnotationItem = BlockContentFeature & {
    active: boolean;
    disabled: boolean;
    icon?: any;
};
declare type Props = {
    blockContentFeatures: BlockContentFeatures;
    editor: SlateEditor;
    editorValue: SlateValue;
    onFocus: (arg0: Path) => void;
    userIsWritingText: boolean;
    collapsed: boolean;
};
export default class AnnotationButtons extends React.Component<Props, {}> {
    shouldComponentUpdate(nextProps: Props): boolean;
    getItems(): AnnotationItem[];
    handleClick: (item: AnnotationItem, originalSelection: any) => void;
    renderAnnotationButton: (item: AnnotationItem) => JSX.Element;
    render(): JSX.Element;
}
export {};
