import React from 'react';
import { BlockContentFeatures, SlateValue, SlateEditor } from '../typeDefs';
export declare type BlockStyleItem = {
    key: string;
    active: boolean;
    title: string;
    style: string;
    preview: Node;
};
declare type Props = {
    blockContentFeatures: BlockContentFeatures;
    editor: SlateEditor;
    editorValue: SlateValue;
    className: string;
};
export default class BlockStyleSelect extends React.Component<Props, {}> {
    shouldComponentUpdate(nextProps: Props): boolean;
    getItemsAndValue(): {
        items: {
            key: string;
            style: string;
            preview: JSX.Element;
            title: string;
            active: any;
        }[];
        value: {
            key: string;
            style: string;
            preview: JSX.Element;
            title: string;
            active: any;
        }[];
    };
    handleChange: (item: BlockStyleItem) => void;
    renderItem: (item: BlockStyleItem) => Node;
    render(): JSX.Element;
}
export {};
