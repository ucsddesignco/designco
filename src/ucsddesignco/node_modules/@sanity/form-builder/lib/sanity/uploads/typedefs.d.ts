import { Type } from '../../typedefs';
import { Patch } from '../../typedefs/patch';
import { Observable } from 'rxjs';
export declare type UploadEvent = {
    type: 'uploadEvent';
    patches: Array<Patch> | null;
};
export declare type ResolvedUploader = {
    uploader: Uploader;
    type: Type;
};
export declare type UploadOptions = {
    metadata?: Array<string> | null;
    storeOriginalFilename?: boolean | null;
};
export declare type UploaderDef = {
    type: string;
    accepts: string;
    upload: (file: File, type: Type) => Observable<UploadEvent>;
};
export declare type Uploader = {
    type: string;
    accepts: string;
    upload: (file: File, type: Type, options?: UploadOptions) => Observable<UploadEvent>;
    priority: number;
};
export declare type UploaderResolver = (type: Type, file: File) => Uploader | null;
